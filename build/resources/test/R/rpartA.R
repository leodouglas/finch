#require(deployrUtils)

#load("rpartA.rdata")
#pattern <- readRDS("test_pattern.rds")

#pattern <- cbind(pattern,pattern_complement)

#decision <- 0
#decision <- predict(rpart.smote.fit.clean,pattern, type = "class")
#decision <- ifelse(decision == 0, "Aprovar", "Rejeitar")
"asd"